[tox]
envlist = py311,py312,py313
skipsdist = false
isolated_build = true

[testenv]
allowlist_externals = uv
commands = python -m pytest -x
deps = pytest
       coverage
       pytest-cov
       pytest-mock
